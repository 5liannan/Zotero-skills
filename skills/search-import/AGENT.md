# AGENT.md — Agent 执行剧本

本目录是一个可由 AI Agent 端到端执行的工作流。
当用户说「帮我做 XX 主题的文献检索并导入 Zotero」时，按本剧本走。
原则：**自动的部分跑完就报告，需要判断的部分停下来问，全程不替用户做取舍**。

## 阶段 0 · 建配置（对话完成）

1. 向用户确认四件事，能推断就先给默认值再复述：
   - 主题关键词（中文 + 英文，含变体、同义词）
   - 是否限定时间范围（Crossref `filter: from-pub-date:`）
   - Zotero 目标分类 key（在 Zotero 里右键分类 → 显示 Key）与要打的标签
   - 有没有明确的排除方向（同一个主词常横跨多个领域，必须问清哪一支不要）
2. 关键词组稿策略：主词 + 变体 + 领域代表学者姓名 + 具体研究体系（材料/模型/应用）
3. 写 `topics/<主题>.json`（参照 `config.example.json`）
4. **向用户复述配置要点，确认后再开跑**

## 阶段 1–2 · 摸底 + 抓取（自动）

- 前置检查：Zotero 是否打开（本地 API 23119；脚本内置 5 次重试，仍失败就提示用户开 Zotero）
- 跑 `01_baseline.mjs <主题>`、`02_search.mjs <主题>`
- 向用户报告：库内现有条数 / 候选总数 / 每组查询命中数（读 `candidates_meta.json`）
- 命中数特别低的查询建议用户换词，改 config 后重跑（自动合并，不重复抓）

## 阶段 3 · 筛选（自动 + 展示）

- 跑 `03_filter.mjs <主题>`
- 用「年份 | 标题 | 期刊 | DOI」表格展示 `filtered_main.json` 全部条目
- 附上 `filter_report.json` 的统计（已在库排除 / 标题相似排除 / 规则排除 / 标题去重）
- 如果规则误伤明显（该留的被排了），回阶段 0 调正则重跑——改 config 即可，不用改代码
- 标题相似排除默认开启（公共词 ≥5 且占短标题 90%+）；先看 `filter_report.json` 的 `simExcluded` 明细，逐条核对"库内对应"是否真是同一篇——阈值调到 0.8 会开始误伤同主题的不同论文。用户明确要保留相似变体时，在 config.filter 里设 `"simInLibrary": false` 重跑

## 检查点 A · 圈定 DOI（人工）

- 逐条和用户过 `filtered_main.json`，把确认要的 DOI 写入 `outputs/<主题>/approved_dois.txt`
- Agent 可以先按用户口头标准预筛一版草稿，但**必须经用户逐条或按批次确认后落盘**
- 顺手问一句：重点文献要不要写一句话注记（如"首次提出该方法""近五年综述"）？要的话写入 `outputs/<主题>/curated.json`（`[{doi, note}]`），阶段 8 的报告会自动按注记分类
- 这个文件就是决策记录：之后想追溯"当时为什么是这批"，答案在这里

## 阶段 4–5 · 定稿 + RIS（自动）

- 跑 `04_finalize.mjs <主题>`、`05_export_ris.mjs <主题>`
- 报告：定稿条数、拉取失败的 DOI（`finalize_errors.json`，可为这些条目手工补 `manual_extra.ris`）
- 给用户 RIS 文件路径（想手动拖 Zotero 的用户在这就能停）

## 检查点 B · 是否直接入库（人工）

- 建议走 `06_import.mjs <主题>`：API 入库 + 自动归到 `collectionKey` 分类 + 打标签
- 用户同意后跑；报告成功 / 失败数
- **失败条目不要慌**：查明原因（多为 Zotero 未开或该条目字段异常）后直接重跑，脚本自动只补失败项

## 阶段 6 · 全文挂载（自动，可选）

- 跑 `07_attach_pdfs.mjs <主题>`
- **必须串行**：脚本本身是一条接一条处理的，不要并发跑多个实例，也不要在 06 入库还没跑完时就开 07——入库高峰并发下载附件会把 Zotero 拖崩，有数据库损坏风险
- PDF 来源：Unpaywall 优先，Optica OE / MDPI 出版社直链兜底
- 报告：attached / no-oa 统计；`no-oa` 是"没能自动拿到 PDF"，不是失败；建议用户在 Zotero 里全选条目右键「查找可用的PDF」补漏
- 失败的重跑即可（断点续跑，已挂载的不会再动）

## 阶段 6.5 · 本地全文回填（自动，可选）

用户手上有 PDF 文件时（07 没拿到的付费文献、中文期刊、会议论文、别人给的打包），走 `09_import_pdfs.mjs <主题> --dir <PDF目录>`

- **先 `--dry` 跑一遍**，把「抽到的 DOI / 补到的标题 / 会不会被判成已在库」念给用户看，确认后再去掉 `--dry` 真跑
- 每条的处理逻辑：PDF 抽 DOI（XMP → 首页文本 → 文件名）→ 查库去重 → 补元数据（本主题定稿 → Crossref by DOI → Crossref by 标题）→ 建条目 + 存 PDF → 归档 + 打标签；元数据全拿不到就交给 Zotero 自己识别 PDF
- **必须向用户说清一条硬限制**：连接器接口只能新建条目，不能给已有条目补附件。所以「库里已有这篇、只是没全文」的 PDF 会被跳过，清单写在 `outputs/<主题>/pdf_existing.txt`，需要用户手动拖进 Zotero。用户坚持要导就加 `--force`，但那会产生重复条目，先提醒再执行
- 同样串行、同样断点续跑（已导入的按文件路径跳过）
- 报告：imported / in-library-with-pdf / in-library-no-pdf / failed 各多少；补上了哪些 07 的 `no-oa` 会单独标出来

## 阶段 7 · 报告生成（自动，可选）

- 跑 `08_report.mjs <主题>`，产出 `report.md` + `references.bib`
- 有 `curated.json` 注记时按 `config.report.categories` 分类（综述/奠基/里程碑等），没有就全部进默认类
- 把 report.md 的分类概览念给用户；注记分类不对时改 config 或 curated.json 重跑，秒级完成

## 收尾

- 汇总一条时间线：查了多少 → 筛出多少 → 定稿多少 → 入库多少 → 挂全文多少 → 本地 PDF 补了多少 → 报告几类
- 提醒用户：`topics/<主题>.json` 与 `outputs/<主题>/` 值得 git 管理（数据文件都在合理体积内）
- 把「主题、关键词组、筛选规则要点、最终条数」写入工作日志，供下次同类任务复用
